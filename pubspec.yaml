name: greenleaf
description: A GreenLeaf recycling app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  syncfusion_flutter_datepicker: ^21.1.40
  intl: ^0.18.1
  shared_preferences: ^2.0.15
  image_picker: ^0.8.6
  #flutter_web_plugins: ^1.0.0 # Adicionado para suporte web

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/